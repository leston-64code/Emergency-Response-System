const express = require("express")
const router = express.Router()

// router.route("/createuser").post(authMiddleware, createAddress)
// router.route("/getuser/:id").delete(authMiddleware, deleteAddress)
// router.route("/getallusers/:id").put(authMiddleware, updateAddress)

module.exports = router